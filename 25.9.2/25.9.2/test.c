#define _CRT_SECURE_NO_WARNINGS
#include"seqlist.h"
int main()
{
	/*SL sl;*/
	/*IntSL(&sl);
	SLpushback(&sl, 2);
	SLpushfront(&sl, 1);
	SLpushback(&sl, 2);
	SLpushfront(&sl, 1);
	SLdelfront(&sl);
	SLdelback(&sl);
	SLinsert(&sl, 0, 99);
	SLdel(&sl, 1);
	printf("%d\n",SLfind(&sl, 99));
	SLprint(&sl);*/
	contact con;
	contactadd(&con);
	contactadd(&con);
	return 0;
}
